;;; extensions.el --- rjhilgefort Layer extensions File for Spacemacs

(setq rjhilgefort-pre-extensions
    '(
    ;; pre extension names go here
    ))

(setq rjhilgefort-post-extensions
    '(
    ;; post extension names go here
    ))

;; For each extension, define a function rjhilgefort/init-<extension-name>
;;
;; (defun rjhilgefort/init-my-extension ()
;;   "Initialize my extension"
;;   )
;;
;; Often the body of an initialize function uses `use-package'
;; For more info on `use-package', see readme:
;; https://github.com/jwiegley/use-package
